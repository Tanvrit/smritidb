# Generated by `gradle wrapper` (Gradle 9.7.1), the fleet toolchain for the
# Kotlin Gradle plugin in use (2.4.20). This file is the ONLY place the
# version is written: CI (.github/workflows/ci.yml and conformance.yml) runs
# ./gradlew on Linux, and on macOS reads distributionUrl and provisions
# exactly that Gradle through setup-gradle.
#
# distributionSha256Sum is the checksum Gradle publishes beside the zip.
# Re-running `gradle wrapper --gradle-version X` rewrites this file and drops
# both this comment and the checksum unless --gradle-distribution-sha256-sum
# is passed as well — re-pin it on every upgrade, then run `./gradlew wrapper`
# once more so gradlew and gradle-wrapper.jar come from the new version too.
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
distributionSha256Sum=acd53f1edaf02f1a8ff99879f8a34b302661a057d9b063ae9e35b552f804d20a
distributionUrl=https\://services.gradle.org/distributions/gradle-9.7.1-bin.zip
networkTimeout=10000
retries=0
retryBackOffMs=500
validateDistributionUrl=true
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
